events {}
http{
    include mime.types;

    server {
        listen 80;

        location / {
            root /usr/share/inginx/html;
            try_files $uri $uri/ /index.html = 404;
        }
    }
}